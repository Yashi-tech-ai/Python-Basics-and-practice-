s = { 1, 2,4, 5,"Yashi"}
print(s,type(s))
# s.add(45) = adds in the set 

s.remove(2) # shows error if not foud

s.discard(4)  #  shows no error if not found

s.pop() # throws random element out 

s.clear() # clears the whole set

s2 = s.copy() # creates a copy

len(s) # gives length 

